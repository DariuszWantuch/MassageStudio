@model ShopProject.Models.ViewModels.HomeViewModel

@foreach (var category in Model.CategoryList.OrderBy(o => o.Name))
{
@if (Model.ServiceList.Where(c => c.CategoryId == category.Id).Count() > 0)
{
<div class="row container pb-3 backgroundWhite">
    <div class="col-12">
        <div class="row">


            <div class="col-12">
                <div class="row my-3">
                    @foreach (var service in Model.ServiceList.Where(c => c.CategoryId == category.Id))
                    {

<div class="col-lg-4 col-md-4 col-sm-6" style="padding-bottom:80px">
    <div class="text-center">
        <div class="tb-product-style-4" >
            <figure>
                <img src="@service.ImageUrl" class="img-responsive rounded-circle" style="border:1px;border-style:solid"/>
                <figcaption class="border">
                    <div class="tb-product4-content" >
                        <h4><b>@service.Name</b></h4>
                        <div class="pl-1 text-dark text-center" style="padding-bottom:10px">
                            <b><i class="fas fa-coins">&nbsp;@service.Price zł&nbsp;&nbsp;</i></b>
                            <b><i class="fas fa-hourglass-half">&nbsp;@service.TimeService.Time</i></b>
                            
                        </div>
                        <a asp-action="Details" class="btn-default" style="border-radius:5px;" asp-route-id="@service.Id">Szczegóły</a>
                    </div>
                </figcaption>
            </figure>
        </div>
    </div>
</div>               }


                </div>
            </div>
        </div>
    </div>
</div>}}